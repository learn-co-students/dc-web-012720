console.log("before timeout");
[1, 2, 3, 5].forEach(x => console.log(x))
console.log("after timeout")

// synchronous
    // runs in order top to bottom
// asynchronous
    // runs after all the synchronous code completes

